{
  "$schema": "https://opencode.ai/config.json",
  "skills": {
    "description": "Skills are auto-discovered from .opencode/skills/<name>/SKILL.md. Run ./setup or setup.ps1 to create the symlink structure."
  }
}
